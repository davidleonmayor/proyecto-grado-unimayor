{
  "scripts": {
    "build:frontend": "pnpm --filter frontend install && pnpm --filter frontend build",
    "build:backend": "pnpm --filter backend install && pnpm --filter backend run build",
    "build": "pnpm run build:backend && pnpm run build:frontend"
  },
  "devDependencies": {
    "@types/blob-stream": "^0.1.33",
    "@types/pdfkit": "^0.17.3"
  },
  "dependencies": {
    "blob-stream": "^0.1.3",
    "jspdf": "^3.0.3",
    "pdfkit": "^0.17.2",
    "xlsx-js-style": "^1.2.0"
  }
}